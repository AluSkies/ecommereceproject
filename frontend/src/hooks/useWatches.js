// Re-export from the canonical catalog feature so both import paths work
export {
  useWatches,
  useAllWatches,
  useFeaturedWatches,
  useWatchById,
  useCategories,
} from '@/features/catalog/useCatalog'
